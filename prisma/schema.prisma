// -----------------------------------------------------------------------------
// @file: prisma/schema.prisma
// @purpose: Brandbite core database schema (roles, tickets, tokens, ledger)
// @version: v2.0.0
// @status: active
// @lastUpdate: 2026-02-20
// -----------------------------------------------------------------------------

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  // Runtime queries use the pooled connection for serverless efficiency.
  url       = env("DATABASE_URL")
  // Migrations need a direct (non-pooled) connection because Prisma relies
  // on Postgres advisory locks to serialize deploys, and connection poolers
  // (PgBouncer / Neon pooler in transaction mode) release session-scoped
  // locks at transaction boundaries. Without this, `prisma migrate deploy`
  // fails with P1002 "Timed out trying to acquire a postgres advisory
  // lock" (observed in Vercel production builds through Neon's pooler).
  //
  // DIRECT_URL must be set in every environment that runs migrations:
  //   - Vercel Production: the non-pooled Neon URL (same host as
  //     DATABASE_URL but WITHOUT "-pooler" in the subdomain)
  //   - CI / local dev / integration tests: fine to mirror DATABASE_URL —
  //     scripts/vercel-build.mjs and tests/integration/setup.ts set this
  //     automatically when unset
  directUrl = env("DIRECT_URL")
}

// -----------------------------------------------------------------------------
// Enums
// -----------------------------------------------------------------------------

enum UserRole {
  SITE_OWNER
  SITE_ADMIN
  DESIGNER // DB value stays "DESIGNER"; display label = "Creative"
  CUSTOMER
}

enum TicketStatus {
  TODO
  IN_PROGRESS
  IN_REVIEW
  DONE
}

enum TicketPriority {
  LOW
  MEDIUM
  HIGH
  URGENT
}

enum LedgerDirection {
  CREDIT
  DEBIT
}

enum WithdrawalStatus {
  PENDING
  APPROVED
  REJECTED
  PAID
}

enum CompanyRole {
  OWNER // company owner
  PM // company-wide project management
  BILLING // accountant / finance
  MEMBER // regular member (default)
}

enum InviteStatus {
  PENDING
  ACCEPTED
  EXPIRED
  CANCELLED
}

/// Project-level role
enum ProjectRole {
  OWNER // project owner (usually company owner / lead)
  PM // project manager
  CONTRIBUTOR // aktif calisan
  VIEWER // read-only
}

enum AutoAssignMode {
  INHERIT
  ON
  OFF
}

enum BillingStatus {
  ACTIVE
  PAST_DUE
  CANCELED
}

// --- Notifications ---

enum NotificationType {
  REVISION_SUBMITTED // Creative submitted a new revision
  FEEDBACK_SUBMITTED // Customer submitted revision feedback (pins)
  TICKET_COMPLETED // Ticket marked as DONE
  TICKET_ASSIGNED // Ticket assigned to creative
  TICKET_STATUS_CHANGED // Any status transition
  PIN_RESOLVED // Creative resolved a pin
}

// --- CMS Content ---

enum CmsStatus {
  DRAFT
  PUBLISHED
}

// --- AI Tools ---

enum TicketCreativeMode {
  DESIGNER
  AI
}

enum AiGenerationStatus {
  PENDING
  PROCESSING
  COMPLETED
  FAILED
}

enum AiToolType {
  IMAGE_GENERATION
  BACKGROUND_REMOVAL
  TEXT_GENERATION
  DESIGN_SUGGESTION
  BRIEF_PARSING
  UPSCALE_IMAGE
}

// --- Assets & Annotations ---

enum AssetKind {
  BRIEF_INPUT
  OUTPUT_IMAGE
}

enum PinStatus {
  OPEN
  RESOLVED
}

enum TagColor {
  GRAY
  BLUE
  GREEN
  ORANGE
  RED
  PURPLE
  PINK
  YELLOW
}

// --- Moodboard ---

enum MoodboardItemType {
  NOTE
  IMAGE
  COLOR
  LINK
  FILE
  TODO
  EMBED
  DRAWING
}

enum TalentApplicationStatus {
  SUBMITTED
  IN_REVIEW
  // Admin clicked "Accept & schedule", three slot offers were emailed to
  // the candidate via a tokenized booking link. Awaiting their pick.
  AWAITING_CANDIDATE_CHOICE
  // Candidate chose "none of the offered slots work" and submitted their
  // own proposed time. Site owner needs to confirm (or counter-propose).
  CANDIDATE_PROPOSED_TIME
  ACCEPTED
  DECLINED
}

// -----------------------------------------------------------------------------
// Core User & Auth
// -----------------------------------------------------------------------------

model UserAccount {
  id         String   @id @default(cuid())
  authUserId String   @unique
  email      String   @unique
  name       String?
  role       UserRole

  /// Soft-delete timestamp. When set, the account is considered anonymized
  /// (email rewritten to a placeholder, name cleared) and auth is refused.
  /// Ledger + ticket rows are retained for financial / audit compliance.
  deletedAt DateTime?

  /// Optional TOTP secret for the L4 MFA second factor. When set, money
  /// actions use TOTP codes from an authenticator app instead of email
  /// codes. Stored as base32 (RFC 4648). Null = not enrolled / email
  /// fallback. Generated + saved via /api/admin/mfa/enroll.
  totpSecret     String?
  totpEnrolledAt DateTime?

  /// Admin-controlled flag: whether this creative can send revision notes to clients
  creativeRevisionNotesEnabled Boolean @default(false) @map("designerRevisionNotesEnabled")

  /// Whether this creative has paused accepting new assignments
  isPaused       Boolean   @default(false)
  /// When the creative activated pause mode
  pausedAt       DateTime?
  /// When the pause expires (null = manual/indefinite pause)
  pauseExpiresAt DateTime?
  /// Pause type: "1_HOUR", "7_DAYS", "MANUAL"
  pauseType      String?

  // Relations
  companies        CompanyMember[]
  creativeTickets  Ticket[]        @relation("CreativeTickets")
  customerTickets  Ticket[]        @relation("CustomerTickets")
  completedTickets Ticket[]        @relation("TicketCompletedBy")

  projectMemberships ProjectMember[]

  companyInvitesSent CompanyInvite[]

  ledgerEntries  TokenLedger[]   @relation("UserLedger")
  withdrawals    Withdrawal[]
  ticketComments TicketComment[]

  assignmentLogs TicketAssignmentLog[]

  submittedRevisions TicketRevision[] @relation("TicketRevisionSubmittedBy")
  feedbackRevisions  TicketRevision[] @relation("TicketRevisionFeedbackBy")

  // Assets & annotations
  createdAssets     Asset[]
  createdAssetPins  AssetPin[]        @relation("AssetPinCreatedBy")
  resolvedAssetPins AssetPin[]        @relation("AssetPinResolvedBy")
  assetPinComments  AssetPinComment[]

  // Notifications
  notifications      Notification[]           @relation("UserNotifications")
  actedNotifications Notification[]           @relation("NotificationActor")
  notificationPrefs  NotificationPreference[]

  // Creative skills (job type capabilities)
  creativeSkills CreativeSkill[] @relation("CreativeSkills")

  // AI generations
  aiGenerations AiGeneration[]

  // Moodboards
  createdMoodboards     Moodboard[]     @relation("MoodboardCreatedBy")
  createdMoodboardItems MoodboardItem[] @relation("MoodboardItemCreatedBy")

  // Creative ratings (admin-only signal; creative does NOT see their own ratings)
  receivedRatings  CreativeRating[] @relation("CreativeRatingsReceived")
  submittedRatings CreativeRating[] @relation("CreativeRatingsGiven")

  // Consultations requested by this user
  requestedConsultations Consultation[] @relation("ConsultationsRequested")

  // Consultation settings rows last touched by this admin (site-level)
  updatedConsultationSettings ConsultationSettings[] @relation("ConsultationSettingsUpdatedBy")

  // Time-tracking entries owned by this creative (D7)
  creativeTimeEntries TicketTimeEntry[] @relation("CreativeTimeEntries")

  // Admin action log entries this user authored (SITE_OWNER only in practice)
  adminActionLogs AdminActionLog[] @relation("AdminActionLogActor")

  // MFA challenges issued to this user (email-code second factor)
  mfaChallenges MfaChallenge[]

  // Co-approvals this user has signed on large-withdrawal requests
  withdrawalCoApprovals WithdrawalApproval[] @relation("WithdrawalCoApprover")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

// -----------------------------------------------------------------------------
// Company & Plans
// -----------------------------------------------------------------------------

model Company {
  id   String @id @default(cuid())
  name String

  slug String @unique

  website String? // Company website URL (optional)

  planId String?
  plan   Plan?   @relation(fields: [planId], references: [id])

  /// Company default auto-assign behaviour:
  /// - true: if project is "INHERIT", auto-assign is considered enabled.
  /// - false: if project is "INHERIT", auto-assign is considered disabled.
  autoAssignDefaultEnabled Boolean @default(false)

  members CompanyMember[]

  projects Project[]

  tickets Ticket[]

  invites CompanyInvite[]

  ledgerEntries TokenLedger[]

  // Tags (company-scoped labels for tickets)
  tags TicketTag[]

  // Output presets (optional company overrides)
  outputSizePresets OutputSizePreset[]

  // Stripe billing mapping
  stripeCustomerId     String?        @unique
  stripeSubscriptionId String?        @unique
  billingStatus        BillingStatus?

  tokenBalance Int @default(0)

  /// Null = onboarding not yet completed (wizard should be shown).
  /// Set to now() once the customer finishes the onboarding wizard.
  onboardingCompletedAt DateTime?

  aiGenerations AiGeneration[]

  moodboards Moodboard[]

  creativeRatings CreativeRating[]

  consultations Consultation[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model CompanyMember {
  id String @id @default(cuid())

  companyId String
  company   Company @relation(fields: [companyId], references: [id])

  userId String
  user   UserAccount @relation(fields: [userId], references: [id])

  /// Company-level role:
  /// - OWNER  : company owner (plan, billing, members)
  /// - PM     : company-wide project / ticket management
  /// - BILLING: accounting / finance
  /// - MEMBER : regular member (default)
  roleInCompany CompanyRole @default(MEMBER)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([companyId, userId])
}

model CompanyInvite {
  id String @id @default(cuid())

  companyId String
  company   Company @relation(fields: [companyId], references: [id])

  email String

  invitedByUserId String?
  invitedByUser   UserAccount? @relation(fields: [invitedByUserId], references: [id])

  /// The role the invitee will receive upon accepting (defaults to MEMBER)
  roleInCompany CompanyRole @default(MEMBER)

  token  String       @unique
  status InviteStatus @default(PENDING)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([companyId])
  @@index([email])
}

model Plan {
  id   String @id @default(cuid())
  name String

  description String?

  stripeProductId String? @unique
  stripePriceId   String? @unique

  monthlyTokens Int
  priceCents    Int

  isActive Boolean @default(true)

  /// Recurring Stripe subscription vs. one-time "top-up" pack. Existing
  /// rows default to true so historical plans keep their subscription
  /// semantics. A top-up pack (false) credits monthlyTokens once when
  /// checkout completes and never writes Company.stripeSubscriptionId.
  isRecurring Boolean @default(true)

  // Plan-level max concurrent IN_PROGRESS ticket count
  maxConcurrentInProgressTickets Int @default(1)

  /// Public display copy for the landing-page pricing card.
  ///
  /// `tagline` is the small line under the plan name ("Best for startups
  /// and solo founders"). `features` is the bullet list rendered with
  /// check icons. `displayCtaLabel` overrides the default "Get Started"
  /// button label per plan. `displaySubtitle` is the small italic line
  /// below the CTA ("Pause or cancel anytime."). `displayOrder` controls
  /// landing-page card order, lower-first; ties fall back to priceCents.
  ///
  /// All optional — landing page renders sensible fallbacks when blank.
  /// Edited via /admin/plans (pricing PR 2 of 2). Stripe stays the
  /// source of truth for `priceCents` and `monthlyTokens`; this column
  /// set is purely marketing copy, decoupled from billing.
  tagline         String?
  features        Json?
  displayCtaLabel String?
  displaySubtitle String?
  displayOrder    Int?

  companies Company[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

// -----------------------------------------------------------------------------
// Projects & Membership
// -----------------------------------------------------------------------------

model Project {
  id        String  @id
  companyId String
  company   Company @relation(fields: [companyId], references: [id])

  name String
  code String? // 3-char uppercase abbreviation (e.g. "WEB")

  autoAssignMode AutoAssignMode @default(INHERIT)

  // Brand guide — surfaced to creatives on every ticket that belongs to this
  // project and spliced into AI prompt context. Customer fills each field once
  // per project instead of repeating brand details on every ticket.
  brandLogoUrl String? /// Single public URL (can be R2, CDN, any https://…).
  brandColors  String? /// Comma-separated hex values, e.g. "#ff6600,#1e293b,#ffffff".
  brandFonts   String? /// Comma-separated font names, e.g. "Inter, Source Serif 4".
  brandVoice   String? /// Free-form tone / voice notes (max 2000 chars at the API level).

  members ProjectMember[]
  tickets Ticket[]

  moodboards Moodboard[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([companyId, code])
  @@index([companyId])
}

model ProjectMember {
  id String @id @default(cuid())

  projectId String
  project   Project @relation(fields: [projectId], references: [id])

  userId String
  user   UserAccount @relation(fields: [userId], references: [id])

  role ProjectRole @default(CONTRIBUTOR)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([projectId, userId])
}

// -----------------------------------------------------------------------------
// Job Type Categories
// -----------------------------------------------------------------------------

model JobTypeCategory {
  id        String  @id @default(cuid())
  name      String  @unique
  slug      String  @unique
  icon      String? // emoji or icon identifier
  sortOrder Int     @default(0)
  isActive  Boolean @default(true)

  jobTypes JobType[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

// -----------------------------------------------------------------------------
// Talent Applications
//
// Public-facing application form at /talent. Anonymous (no UserAccount) —
// the email + WhatsApp number are the contact channels. Status transitions
// (SUBMITTED → ACCEPTED / DECLINED) are driven from the SITE_OWNER admin
// queue in PR2. The multi-value fields (categoryIds, socialLinks, workedWith,
// tools) live as JSON arrays because nothing queries against them; admin
// list filters are by status + createdAt only. JobTypeCategory is the
// source of truth for the skills multi-select; the form posts category IDs
// and the API cross-validates that they're still active before insert.
// -----------------------------------------------------------------------------

model TalentApplication {
  id String @id @default(cuid())

  // Basic info — all required at submit time.
  fullName       String
  whatsappNumber String
  email          String
  country        String
  timezone       String

  // Portfolio & presence
  portfolioUrl String
  linkedinUrl  String?
  /// Array of additional social URLs (Instagram / Behance / Dribbble / …).
  /// Capped at 3 client-side and validated server-side.
  socialLinks  Json    @default("[]")

  /// JobTypeCategory.id values the candidate selected. Min 3 enforced at
  /// submit; cross-validated against `isActive: true` rows so a tampered
  /// payload referencing a deleted/disabled category is rejected.
  categoryIds Json @default("[]")

  // Experience snapshot
  totalYears   String // bucket: "0-2" | "2-5" | "5-10" | "10+"
  hasRemoteExp Boolean
  yearsRemote  String? // null when hasRemoteExp = false
  /// Subset of {STARTUPS,AGENCIES,CORPORATE,FREELANCE}; min 1 enforced.
  workedWith   Json    @default("[]")

  // Availability
  workload              String // "PART_TIME" | "FULL_TIME"
  preferredTasksPerWeek String? // null when PART_TIME

  // Turnaround reality check
  turnaroundOk      Boolean
  turnaroundComment String  @default("") // <= 120 chars

  // Tools & stack
  /// Subset of {FIGMA,ADOBE,WEBFLOW,CANVA,AI_TOOLS,OTHER}; min 1 enforced.
  tools      Json    @default("[]")
  toolsOther String? // populated only when "OTHER" is among `tools`

  // Test task + final confirmation
  testTaskOk             Boolean
  communicationConfirmed Boolean

  // Audit / lifecycle
  status    TalentApplicationStatus @default(SUBMITTED)
  /// IP + user-agent captured at submit for spam triage. Not exposed to
  /// the candidate; visible to SITE_OWNER in the admin detail panel.
  ipAddress String?
  userAgent String?

  /// Admin who actioned the most recent state transition (ACCEPTED /
  /// DECLINED). Null while status is SUBMITTED. Not a relation — actor
  /// rows can be hard-deleted (USER_HARD_DELETE) and we want the stamp
  /// to outlive the actor account, mirroring AdminActionLog.actorEmail
  /// snapshot behavior.
  reviewedAt          DateTime?
  reviewedByUserId    String?
  reviewedByUserEmail String?

  /// Interview booking — populated by the accept flow. `googleEventId`
  /// is the Calendar event id we'd hit to cancel/update; `meetLink` is
  /// the Hangouts URL extracted via lib/google/calendar.ts:extractMeetLink;
  /// `interviewAt` is the UTC start instant for sorting and display.
  /// All three are written together inside the same Prisma transaction
  /// as the status flip — partial state ("ACCEPTED but no event") would
  /// be a regression.
  googleEventId String?
  meetLink      String?
  interviewAt   DateTime?

  /// Optional admin-supplied note on the decline. Surfaced verbatim in
  /// the decline email so the rejection can be soft-tailored without
  /// opening a separate inbox thread. Null/empty falls back to the
  /// generic template copy.
  declineReason String?

  // ---------------------------------------------------------------------
  // 3-slot booking (PR4) — tokenized self-service scheduling.
  //
  // When the admin clicks "Accept & schedule", we no longer create a
  // Google Calendar event immediately. Instead we write three proposed
  // slot ISOs + a single-use bookingToken, set status to
  // AWAITING_CANDIDATE_CHOICE, and email the candidate a tokenized link.
  // The candidate either picks one of the three (→ ACCEPTED, calendar
  // event created at that point) or proposes their own time
  // (→ CANDIDATE_PROPOSED_TIME, admin notified, admin's "accept proposed"
  // action then creates the event).
  //
  // All four columns are nullable so existing PR2-era rows in ACCEPTED
  // /DECLINED keep working without backfill.
  // ---------------------------------------------------------------------

  /// JSON array of up to three proposed slot ISO strings (UTC). Set when
  /// status moves to AWAITING_CANDIDATE_CHOICE; cleared (kept for audit)
  /// once status reaches ACCEPTED or DECLINED.
  proposedSlotsJson Json?

  /// Single-use, plain (not hashed) booking token. The candidate gets
  /// it inside an email link; the public /talent/schedule/[token] page
  /// looks the row up by this column. Plain matches the CompanyInvite
  /// pattern — simpler, and the surface area is already minimal (one
  /// row, one URL, single-use, expiring).
  bookingToken          String?   @unique
  bookingTokenExpiresAt DateTime? // 7 days from creation

  /// Admin-supplied personal note included in the acceptance email above
  /// the slot offer block. Free-form, max 500 chars enforced at the
  /// schema layer.
  customMessage String?

  /// When the candidate proposed a custom time (filled the "none of
  /// these work?" form). The proposed instant is stored separately on
  /// `interviewAt` so we don't need a fifth nullable column — at this
  /// status interviewAt represents the candidate's *proposal*, not a
  /// confirmed booking. The admin's accept-proposed action then keeps
  /// it as-is and creates the calendar event.
  candidateProposedAt DateTime?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([status, createdAt(sort: Desc)])
  @@index([email])
  @@index([interviewAt]) // PR2 admin filter "show upcoming interviews"
}

// -----------------------------------------------------------------------------
// Job Types
// -----------------------------------------------------------------------------

model JobType {
  id String @id @default(cuid())

  name                 String
  category             String? // legacy free-text category (kept for migration)
  categoryId           String?
  categoryRef          JobTypeCategory? @relation(fields: [categoryId], references: [id])
  description          String?
  tokenCost            Int
  creativePayoutTokens Int              @map("designerPayoutTokens")
  estimatedHours       Int? // from service catalog (for reference)

  // Quantity support — some jobs are per-unit (e.g. banner sizes, pages)
  hasQuantity     Boolean @default(false) // show quantity stepper in form
  quantityLabel   String? // e.g. "Number of sizes", "Number of pages"
  defaultQuantity Int     @default(1) // pre-filled quantity for this job type

  /// Optional per-job-type prompt template. Spliced into AI image-generation
  /// prompts as additional context so each job type gets tuned output
  /// (e.g. "For logos: emphasize simplicity, recognizable at small sizes").
  /// Free-form text; empty / null means the generic prompt is used.
  aiPromptTemplate String?

  isActive Boolean @default(true)

  tickets Ticket[]

  // Output size presets (global presets live here; company overrides also relate here)
  outputSizePresets OutputSizePreset[]

  creativeSkills CreativeSkill[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([categoryId])
}

// -----------------------------------------------------------------------------
// Creative Skills (many-to-many: UserAccount <-> JobType)
// -----------------------------------------------------------------------------

model CreativeSkill {
  id String @id @default(cuid())

  creativeId String      @map("designerId")
  creative   UserAccount @relation("CreativeSkills", fields: [creativeId], references: [id])

  jobTypeId String
  jobType   JobType @relation(fields: [jobTypeId], references: [id])

  createdAt DateTime @default(now())

  @@unique([creativeId, jobTypeId])
  @@index([creativeId])
  @@index([jobTypeId])
  @@map("DesignerSkill")
}

// -----------------------------------------------------------------------------
// Output size presets & ticket output specs (quantity/sizes)
// -----------------------------------------------------------------------------

model OutputSizePreset {
  id String @id @default(cuid())

  // Optional company override; null means "global preset"
  companyId String?
  company   Company? @relation(fields: [companyId], references: [id])

  jobTypeId String
  jobType   JobType @relation(fields: [jobTypeId], references: [id])

  /// Stable identifier for code usage (e.g. "IG_POST_1080")
  key   String
  /// UI label (English)
  label String

  width  Int
  height Int
  unit   String @default("px")

  sortOrder Int     @default(0)
  isActive  Boolean @default(true)

  ticketSpecs TicketOutputSpec[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([companyId, jobTypeId, key])
  @@index([jobTypeId, isActive, sortOrder])
}

model TicketOutputSpec {
  id String @id @default(cuid())

  ticketId String
  ticket   Ticket @relation(fields: [ticketId], references: [id])

  presetId String
  preset   OutputSizePreset @relation(fields: [presetId], references: [id])

  quantity Int

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([ticketId, presetId])
  @@index([ticketId])
}

// -----------------------------------------------------------------------------
// Tickets & Comments
// -----------------------------------------------------------------------------

model Ticket {
  id String @id @default(cuid())

  title       String
  description String?

  status       TicketStatus       @default(TODO)
  priority     TicketPriority     @default(MEDIUM)
  creativeMode TicketCreativeMode @default(DESIGNER)
  dueDate      DateTime?

  companyId String
  company   Company @relation(fields: [companyId], references: [id])

  projectId String?
  project   Project? @relation(fields: [projectId], references: [id])

  // User who created the ticket (customer)
  createdById String
  createdBy   UserAccount @relation("CustomerTickets", fields: [createdById], references: [id])

  // Assigned creative
  creativeId String?      @map("designerId")
  creative   UserAccount? @relation("CreativeTickets", fields: [creativeId], references: [id])

  jobTypeId String?
  jobType   JobType? @relation(fields: [jobTypeId], references: [id])

  // Quantity multiplier (default 1). Effective cost = jobType.tokenCost * quantity.
  quantity Int @default(1)

  // Admin overrides — when set, these replace the calculated (jobType cost * quantity).
  tokenCostOverride      Int?
  creativePayoutOverride Int? @map("designerPayoutOverride")

  // Completion tracking (who approved the final revision)
  completedAt   DateTime?
  completedById String?
  completedBy   UserAccount? @relation("TicketCompletedBy", fields: [completedById], references: [id])

  // Company-scoped ticket number
  companyTicketNumber Int?
  revisionCount       Int              @default(0)
  revisions           TicketRevision[]

  // Output specs (quantity/sizes)
  outputSpecs TicketOutputSpec[]

  // Assets (brief inputs + revision outputs)
  assets Asset[]

  ledgerEntries TokenLedger[]
  comments      TicketComment[]

  assignmentLogs TicketAssignmentLog[]
  notifications  Notification[]

  tagAssignments TicketTagAssignment[]

  aiGenerations AiGeneration[]

  moodboards Moodboard[]

  // Creative rating (one per ticket, submitted by the customer on ticket DONE)
  creativeRating CreativeRating?

  // Time-tracking entries (D7). One ticket can have many entries from its
  // assigned creative; exactly zero or one of those is "running" (endedAt is
  // null) at any time, enforced by application logic.
  timeEntries TicketTimeEntry[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([creativeId, status, updatedAt])
}

// -----------------------------------------------------------------------------
// Time tracking (D7)
//
// A creative can start/stop a timer while working on a ticket. Stopped
// entries carry a denormalized durationSeconds so admin analytics can
// aggregate with a single SQL sum. Running entries (endedAt = null) have
// durationSeconds = 0 and are computed client-side against `now` for live
// display. The (creativeId, endedAt = NULL) pair is effectively unique —
// starting a new timer always closes any running one first.
// -----------------------------------------------------------------------------

model TicketTimeEntry {
  id String @id @default(cuid())

  ticketId   String
  ticket     Ticket      @relation(fields: [ticketId], references: [id], onDelete: Cascade)
  creativeId String
  creative   UserAccount @relation("CreativeTimeEntries", fields: [creativeId], references: [id])

  startedAt       DateTime
  endedAt         DateTime?
  durationSeconds Int       @default(0) // 0 while running; set on stop
  notes           String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([ticketId, startedAt])
  @@index([creativeId, endedAt])
}

// -----------------------------------------------------------------------------
// Creative Ratings
//
// Customers rate the creative after a ticket transitions to DONE. Multi-dim
// scores (quality / communication / speed, each 1-5) plus optional free-text
// feedback. Admin-only surface: creatives never see their own ratings. Used
// to (a) inform admin coaching & staffing decisions, and (b) serve as a
// tie-breaker in the auto-assign algorithm.
// -----------------------------------------------------------------------------

model CreativeRating {
  id String @id @default(cuid())

  // One rating per ticket (the customer rates the creative once, on DONE)
  ticketId String @unique
  ticket   Ticket @relation(fields: [ticketId], references: [id], onDelete: Cascade)

  // Creative being rated
  creativeId String
  creative   UserAccount @relation("CreativeRatingsReceived", fields: [creativeId], references: [id])

  // User who submitted the rating (a company member, typically the ticket creator)
  ratedByUserId String
  ratedBy       UserAccount @relation("CreativeRatingsGiven", fields: [ratedByUserId], references: [id])

  // Scope — useful for company-level analytics
  companyId String
  company   Company @relation(fields: [companyId], references: [id])

  // Multi-dimensional scores (1..5). All three required.
  quality       Int
  communication Int
  speed         Int

  // Optional feedback text, admin-only readable.
  feedback String?

  createdAt DateTime @default(now())

  @@index([creativeId, createdAt])
  @@index([companyId, createdAt])
}

// -----------------------------------------------------------------------------
// Tags (company-scoped labels for tickets)
// -----------------------------------------------------------------------------

model TicketTag {
  id    String   @id @default(cuid())
  name  String
  color TagColor @default(GRAY)

  companyId String
  company   Company @relation(fields: [companyId], references: [id])

  assignments TicketTagAssignment[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([companyId, name])
  @@index([companyId])
}

model TicketTagAssignment {
  id String @id @default(cuid())

  ticketId String
  ticket   Ticket @relation(fields: [ticketId], references: [id])

  tagId String
  tag   TicketTag @relation(fields: [tagId], references: [id])

  createdAt DateTime @default(now())

  @@unique([ticketId, tagId])
  @@index([ticketId])
  @@index([tagId])
}

model TicketComment {
  id String @id @default(cuid())

  ticketId String
  ticket   Ticket @relation(fields: [ticketId], references: [id])

  authorId String
  author   UserAccount @relation(fields: [authorId], references: [id])

  body String

  createdAt DateTime @default(now())

  @@index([ticketId])
  @@index([authorId])
}

model TicketRevision {
  id String @id @default(cuid())

  ticketId String
  ticket   Ticket @relation(fields: [ticketId], references: [id])

  /// Revision number for this ticket (v1, v2, v3, ...)
  version Int

  submittedByCreativeId String?      @map("submittedByDesignerId")
  submittedByCreative   UserAccount? @relation("TicketRevisionSubmittedBy", fields: [submittedByCreativeId], references: [id])

  submittedAt     DateTime @default(now())
  creativeMessage String?  @map("designerMessage")

  feedbackByCustomerId String?
  feedbackByCustomer   UserAccount? @relation("TicketRevisionFeedbackBy", fields: [feedbackByCustomerId], references: [id])

  feedbackAt      DateTime?
  feedbackMessage String?

  /// When non-null, this revision was generated by AI (not a human designer)
  aiGenerationId String?
  aiGeneration   AiGeneration? @relation(fields: [aiGenerationId], references: [id])

  // Assets that belong to this revision (OUTPUT_IMAGE usually)
  assets Asset[]

  createdAt DateTime @default(now())

  @@unique([ticketId, version])
  @@index([ticketId, version])
  @@index([submittedByCreativeId, submittedAt])
  @@index([feedbackByCustomerId, feedbackAt])
}

// -----------------------------------------------------------------------------
// Assets & Pins (annotations)
// -----------------------------------------------------------------------------

model Asset {
  id String @id @default(cuid())

  ticketId String
  ticket   Ticket @relation(fields: [ticketId], references: [id])

  /// OUTPUT_IMAGE assets typically belong to a revision; BRIEF_INPUT can be null
  revisionId String?
  revision   TicketRevision? @relation(fields: [revisionId], references: [id])

  kind AssetKind

  /// Storage metadata (S3 key / UploadThing key / blob id)
  storageKey String
  url        String?
  mimeType   String
  bytes      Int

  width        Int?
  height       Int?
  originalName String?

  createdById String
  createdBy   UserAccount @relation(fields: [createdById], references: [id])

  createdAt DateTime  @default(now())
  deletedAt DateTime?

  pins AssetPin[]

  @@index([ticketId, kind, createdAt])
  @@index([revisionId])
  @@index([createdById, createdAt])
}

model AssetPin {
  id String @id @default(cuid())

  assetId String
  asset   Asset  @relation(fields: [assetId], references: [id])

  createdById String
  createdBy   UserAccount @relation("AssetPinCreatedBy", fields: [createdById], references: [id])

  /// Normalized coordinates (0..1)
  x Float
  y Float

  /// UI order/number (1..N) per asset
  order  Int
  label  String?
  status PinStatus @default(OPEN)

  resolvedAt   DateTime?
  resolvedById String?
  resolvedBy   UserAccount? @relation("AssetPinResolvedBy", fields: [resolvedById], references: [id])

  comments AssetPinComment[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([assetId, order])
  @@index([assetId, status])
  @@index([createdById, createdAt])
}

model AssetPinComment {
  id String @id @default(cuid())

  pinId String
  pin   AssetPin @relation(fields: [pinId], references: [id])

  authorId String
  author   UserAccount @relation(fields: [authorId], references: [id])

  message String

  createdAt DateTime @default(now())

  @@index([pinId, createdAt])
  @@index([authorId, createdAt])
}

// -----------------------------------------------------------------------------
// Tokens & Ledger
// -----------------------------------------------------------------------------

model TokenLedger {
  id String @id @default(cuid())

  companyId String?
  company   Company? @relation(fields: [companyId], references: [id])

  ticketId String?
  ticket   Ticket? @relation(fields: [ticketId], references: [id])

  userId String?
  user   UserAccount? @relation("UserLedger", fields: [userId], references: [id])

  direction LedgerDirection
  amount    Int
  reason    String?
  notes     String?
  metadata  Json?

  balanceBefore Int?
  balanceAfter  Int?

  createdAt DateTime @default(now())

  @@index([companyId, createdAt])
  @@index([userId, createdAt])
  @@index([ticketId, createdAt])
}

// -----------------------------------------------------------------------------
// Withdrawals
// -----------------------------------------------------------------------------

model Withdrawal {
  id String @id @default(cuid())

  creativeId String      @map("designerId")
  creative   UserAccount @relation(fields: [creativeId], references: [id])

  amountTokens Int
  status       WithdrawalStatus @default(PENDING)

  notes    String?
  metadata Json?

  createdAt  DateTime  @default(now())
  approvedAt DateTime?
  paidAt     DateTime?

  approvals WithdrawalApproval[]

  @@index([creativeId, createdAt])
  @@index([status, createdAt])
}

// -----------------------------------------------------------------------------
// 2-person approval on large withdrawals (Security Precaution Plan — L6)
//
// Above a threshold, a single SITE_OWNER approving a withdrawal is NOT
// enough to flip it to APPROVED. Two distinct owners must each click
// Approve (with L2 confirmation + L4 MFA each time). The first approval
// records a row here; the second transitions the withdrawal.
//
// If there is only one SITE_OWNER on the platform, the coapproval check
// bypasses cleanly (otherwise large withdrawals would freeze forever).
// The bypass itself writes an AdminActionLog entry so it's visible.
// -----------------------------------------------------------------------------

model WithdrawalApproval {
  id String @id @default(cuid())

  withdrawalId String
  withdrawal   Withdrawal @relation(fields: [withdrawalId], references: [id], onDelete: Cascade)

  approverId String
  approver   UserAccount @relation("WithdrawalCoApprover", fields: [approverId], references: [id])

  ipAddress String?
  userAgent String?

  createdAt DateTime @default(now())

  // Same owner can't sign the same withdrawal twice — forces two distinct
  // owners for the 2-person rule to complete.
  @@unique([withdrawalId, approverId])
  @@index([withdrawalId])
}

// -----------------------------------------------------------------------------
// Notifications
// -----------------------------------------------------------------------------

model Notification {
  id String @id @default(cuid())

  userId String
  user   UserAccount @relation("UserNotifications", fields: [userId], references: [id])

  type NotificationType

  title   String
  message String

  ticketId String?
  ticket   Ticket? @relation(fields: [ticketId], references: [id])

  /// The user who triggered this notification (creative or customer)
  actorId String?
  actor   UserAccount? @relation("NotificationActor", fields: [actorId], references: [id])

  read   Boolean   @default(false)
  readAt DateTime?

  createdAt DateTime @default(now())

  @@index([userId, read, createdAt])
  @@index([userId, createdAt])
}

model NotificationPreference {
  id String @id @default(cuid())

  userId String
  user   UserAccount @relation(fields: [userId], references: [id])

  type         NotificationType
  enabled      Boolean          @default(true)
  emailEnabled Boolean          @default(true)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([userId, type])
}

// -----------------------------------------------------------------------------
// App Settings (key-value store for admin-configurable values)
// -----------------------------------------------------------------------------

model AppSetting {
  key   String @id
  value String

  updatedAt DateTime @updatedAt
}

// -----------------------------------------------------------------------------
// Payout Rules (admin-configurable creative payout milestones)
// -----------------------------------------------------------------------------

model PayoutRule {
  id                  String   @id @default(cuid())
  name                String
  description         String?
  minCompletedTickets Int
  timeWindowDays      Int
  payoutPercent       Int
  priority            Int      @default(0)
  isActive            Boolean  @default(true)
  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt

  @@index([isActive, payoutPercent])
}

// -----------------------------------------------------------------------------
// Ticket Assignment Logs
// -----------------------------------------------------------------------------

model TicketAssignmentLog {
  id String @id @default(cuid())

  ticketId String
  ticket   Ticket @relation(fields: [ticketId], references: [id])

  // Which creative was assigned to the ticket?
  creativeId String?      @map("designerId")
  creative   UserAccount? @relation(fields: [creativeId], references: [id])

  /// reason examples:
  /// - AUTO_ASSIGN
  /// - MANUAL
  /// - FALLBACK
  /// - REBALANCE
  /// - SYSTEM_MIGRATION
  reason   String
  notes    String?
  metadata Json?

  createdAt DateTime @default(now())

  @@index([ticketId, createdAt])
  @@index([creativeId, createdAt])
  @@index([reason, createdAt])
}

// -----------------------------------------------------------------------------
// CMS — Showcase & Blog (admin-managed marketing content)
// -----------------------------------------------------------------------------

model ShowcaseWork {
  id String @id @default(cuid())

  title String
  slug  String @unique

  subtitle   String? // e.g. "Brand Identity for Acme Corp"
  clientName String?
  category   String? // e.g. "Brand Identity", "Web Design"
  tags       String[] @default([])

  // Images — stored as R2 storage keys + resolved public URLs
  thumbnailStorageKey String?
  thumbnailUrl        String?
  heroStorageKey      String?
  heroUrl             String?
  galleryImages       Json? // [{storageKey, url, alt}]

  // Content
  description String? // Rich HTML

  // Publishing
  status      CmsStatus @default(DRAFT)
  publishedAt DateTime?
  sortOrder   Int       @default(0)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([status, sortOrder])
}

model BlogPost {
  id String @id @default(cuid())

  title String
  slug  String @unique

  excerpt    String? // Plain-text summary for listing cards
  authorName String?
  category   String? // e.g. "Design Tips", "Company News"
  tags       String[] @default([])

  // Images
  thumbnailStorageKey String?
  thumbnailUrl        String?
  heroStorageKey      String?
  heroUrl             String?

  // Content
  body String? // Rich HTML

  // SEO
  metaTitle       String?
  metaDescription String?

  // Publishing
  status      CmsStatus @default(DRAFT)
  publishedAt DateTime?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([status, publishedAt])
}

// -----------------------------------------------------------------------------
// CMS Pages — single-page content (About, Contact, Documentation)
// -----------------------------------------------------------------------------

model CmsPage {
  id              String   @id @default(cuid())
  pageKey         String   @unique // "about", "contact", "documentation"
  title           String
  subtitle        String?
  heroStorageKey  String?
  heroUrl         String?
  body            String? // Rich HTML
  metaTitle       String?
  metaDescription String?
  updatedAt       DateTime @updatedAt
}

/// Block-based page editor. Used by the marketing landing page (and
/// eventually any other CMS-editable page) to compose sections out of
/// a small library of typed components: hero, feature grid, FAQ,
/// pricing, etc.
///
/// The `data` JSON is Zod-validated against per-type schemas defined
/// in `lib/blocks/types.ts`. Server-side rendering and the admin
/// editor both go through that registry, so adding a new block type
/// is one schema entry plus a render component plus an admin form.
///
/// `pageKey` matches `CmsPage.pageKey` semantics (e.g. "home", "about")
/// but is intentionally NOT a foreign key — legal pages stay on the
/// rich-text `CmsPage.body` path; marketing/landing pages move onto
/// blocks. The two systems coexist.
model PageBlock {
  id        String   @id @default(cuid())
  pageKey   String // "home", future: "about", etc.
  /// Ordering within a page. Unique per (pageKey, position) so
  /// reordering is a swap of two rows. Drag-and-drop reorder support
  /// is a Phase 3 follow-up.
  position  Int
  /// Block type discriminator. String not enum so adding new types is
  /// a code change, not a migration. See `lib/blocks/types.ts` for
  /// the closed set.
  type      String
  /// Block-specific payload. Validated by the Zod schema for `type`
  /// before save. Examples:
  ///   HERO   → { headline, subhead?, ctaLabel?, ctaHref?,
  ///             imageStorageKey?, imageUrl?, variant }
  ///   FAQ    → { title?, qas: [{ q, a }] }
  ///   FEATURE_GRID → { title?, subtitle?, items: [{ title, body, emoji? }] }
  data      Json
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([pageKey, position])
  @@index([pageKey])
}

// -----------------------------------------------------------------------------
// FAQ — central question/answer store, shared across all FAQ surfaces.
//
// One row per question. Used by:
//   - Public /faq marketing page
//   - /customer/faq, /creative/faq dashboard pages
//   - Landing-page PageBlock (FAQ block, which stores selected Faq IDs)
//
// Categories live as a free-form string so adding a new category is data,
// not a code change. The shipped set is "General", "Pricing & Billing",
// "Creative Process", "Platform & Tools", "Agencies & Teams" — anything
// else is fine, FaqBrowser pulls the unique set from the API.
// -----------------------------------------------------------------------------

model Faq {
  id        String   @id @default(cuid())
  /// The user-facing question. Max ~200 chars in the admin UI.
  question  String
  /// The user-facing answer. Markdown not rendered yet; line breaks
  /// preserved via whitespace-pre-line on the renderer side.
  answer    String
  /// Category bucket the FAQ falls under, e.g. "Pricing & Billing".
  /// Free-form string so adding a new category is a CRUD action, not a
  /// schema change.
  category  String
  /// Manual ordering within a category. Lower renders first. We sort by
  /// (category, position) at query time so admins control the order.
  position  Int      @default(0)
  /// Soft-delete / hide flag. Inactive items don't appear on any
  /// public surface but stay in the admin list for restore.
  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([category, position])
  @@index([isActive])
}

// -----------------------------------------------------------------------------
// News Articles — listing CMS (like BlogPost)
// -----------------------------------------------------------------------------

model NewsArticle {
  id                  String    @id @default(cuid())
  title               String
  slug                String    @unique
  excerpt             String?
  authorName          String?
  category            String?
  tags                String[]  @default([])
  thumbnailStorageKey String?
  thumbnailUrl        String?
  heroStorageKey      String?
  heroUrl             String?
  body                String? // Rich HTML
  metaTitle           String?
  metaDescription     String?
  status              CmsStatus @default(DRAFT)
  publishedAt         DateTime?
  createdAt           DateTime  @default(now())
  updatedAt           DateTime  @updatedAt

  @@index([status, publishedAt])
}

// -----------------------------------------------------------------------------
// Documentation Center — Categories & Articles
// -----------------------------------------------------------------------------

enum DocAudience {
  CREATIVE
  CUSTOMER
  GENERAL
}

model DocCategory {
  id          String      @id @default(cuid())
  title       String
  slug        String      @unique
  description String?
  icon        String? // emoji or icon identifier
  audience    DocAudience @default(GENERAL)
  sortOrder   Int         @default(0)

  articles DocArticle[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([audience, sortOrder])
}

model DocArticle {
  id      String  @id @default(cuid())
  title   String
  slug    String  @unique
  excerpt String?
  body    String? // Rich HTML

  categoryId String
  category   DocCategory @relation(fields: [categoryId], references: [id])

  authorName      String?
  status          CmsStatus @default(DRAFT)
  sortOrder       Int       @default(0)
  publishedAt     DateTime?
  metaTitle       String?
  metaDescription String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([categoryId, status, sortOrder])
  @@index([status, publishedAt])
}

// -----------------------------------------------------------------------------
// AI Generations
// -----------------------------------------------------------------------------

model AiGeneration {
  id       String     @id @default(cuid())
  toolType AiToolType

  userId String
  user   UserAccount @relation(fields: [userId], references: [id])

  companyId String
  company   Company @relation(fields: [companyId], references: [id])

  /// Optional ticket context (null for standalone AI Tools usage)
  ticketId String?
  ticket   Ticket? @relation(fields: [ticketId], references: [id])

  /// The prompt/input sent to the AI provider
  prompt      String
  /// Full parameters sent (model, size, style, etc.)
  inputParams Json?

  /// Provider response details
  provider String // "openai" | "replicate"
  model    String // "dall-e-3", "flux-pro", "gpt-4o", "rmbg-2.0"

  status AiGenerationStatus @default(PENDING)

  /// Client-supplied idempotency key (UUID). A retry of the same submission
  /// with the same key returns the existing generation instead of debiting
  /// tokens again. Unique when present; null for legacy / server-only calls.
  idempotencyKey String? @unique

  /// AI output (text, URLs, suggestions JSON)
  outputText     String?
  outputImageUrl String?
  outputParams   Json?

  /// Cost tracking
  tokenCost    Int   @default(0)
  providerCost Json? // Actual API cost details for internal tracking

  /// Error handling
  errorMessage String?
  retryCount   Int     @default(0)

  /// Timing
  startedAt   DateTime?
  completedAt DateTime?
  createdAt   DateTime  @default(now())

  /// Relations
  revisions TicketRevision[]

  @@index([userId, createdAt])
  @@index([companyId, createdAt])
  @@index([ticketId, createdAt])
  @@index([toolType, status])
}

model AiToolConfig {
  id       String     @id @default(cuid())
  toolType AiToolType @unique

  /// Whether this tool is currently enabled
  enabled Boolean @default(true)

  /// Token cost per operation
  tokenCost Int @default(1)

  /// Rate limiting (max operations per company per hour)
  rateLimit Int @default(20)

  /// Provider config overrides (model version, size defaults, etc.)
  config Json?

  updatedAt DateTime @updatedAt
}

// -----------------------------------------------------------------------------
// BetterAuth Tables (managed by BetterAuth — do not modify manually)
// -----------------------------------------------------------------------------

model AuthUser {
  id            String   @id
  name          String
  email         String   @unique
  emailVerified Boolean  @default(false)
  image         String?
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  sessions AuthSession[]
  accounts AuthAccount[]

  @@map("user")
}

model AuthSession {
  id        String   @id
  expiresAt DateTime
  token     String   @unique
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  ipAddress String?
  userAgent String?

  userId String
  user   AuthUser @relation(fields: [userId], references: [id])

  @@map("session")
}

model AuthAccount {
  id                    String    @id
  accountId             String
  providerId            String
  userId                String
  accessToken           String?
  refreshToken          String?
  idToken               String?
  accessTokenExpiresAt  DateTime?
  refreshTokenExpiresAt DateTime?
  scope                 String?
  password              String?
  createdAt             DateTime  @default(now())
  updatedAt             DateTime  @updatedAt

  user AuthUser @relation(fields: [userId], references: [id])

  @@map("account")
}

model AuthVerification {
  id         String    @id
  identifier String
  value      String
  expiresAt  DateTime
  createdAt  DateTime? @default(now())
  updatedAt  DateTime? @updatedAt

  @@map("verification")
}

// -----------------------------------------------------------------------------
// Moodboard
// -----------------------------------------------------------------------------

model Moodboard {
  id          String  @id @default(cuid())
  title       String
  description String?

  companyId String
  company   Company @relation(fields: [companyId], references: [id])

  projectId String?
  project   Project? @relation(fields: [projectId], references: [id])

  ticketId String?
  ticket   Ticket? @relation(fields: [ticketId], references: [id])

  createdById String
  createdBy   UserAccount @relation("MoodboardCreatedBy", fields: [createdById], references: [id])

  items       MoodboardItem[]
  connections Json            @default("[]")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([companyId, createdAt])
  @@index([projectId])
  @@index([ticketId])
}

model MoodboardItem {
  id String @id @default(cuid())

  moodboardId String
  moodboard   Moodboard @relation(fields: [moodboardId], references: [id], onDelete: Cascade)

  type     MoodboardItemType
  position Int               @default(0)
  colSpan  Int               @default(1)

  /// Freeform canvas position and size (logical pixels)
  x      Float @default(0)
  y      Float @default(0)
  width  Float @default(280)
  height Float @default(0) /// 0 = auto-height (content determines)

  /// Polymorphic JSON data — shape depends on `type`:
  /// NOTE:  { title?, body }
  /// IMAGE: { url, storageKey, caption?, width?, height?, originalName?, mimeType?, bytes? }
  /// COLOR: { hex, name? }
  /// LINK:  { url, title?, description?, favicon? }
  /// FILE:  { url, storageKey, originalName, mimeType, bytes }
  /// TODO:  { title?, items: [{ id, text, checked }] }
  data Json

  createdById String
  createdBy   UserAccount @relation("MoodboardItemCreatedBy", fields: [createdById], references: [id])

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([moodboardId, position])
}

// -----------------------------------------------------------------------------
// Consultations
//
// Company OWNER / PM members can request a video consultation with the
// Brandbite admin team. Costs tokens up-front (debited via TokenLedger on
// submit, refunded on admin/company cancel). Status lifecycle:
//   PENDING    — customer submitted, admin hasn't scheduled yet
//   SCHEDULED  — admin picked a time + video link, customer notified
//   COMPLETED  — call happened (manually marked by admin)
//   CANCELED   — either side backed out; refund posted if we had debited
//
// MVP deliberately keeps scheduling out-of-band: the admin pastes a
// Google Meet / Zoom link into `videoLink` when flipping to SCHEDULED.
// A later PR can swap this for a Cal.com-style embed without changing
// the data model.
// -----------------------------------------------------------------------------

enum ConsultationStatus {
  PENDING
  SCHEDULED
  COMPLETED
  CANCELED
}

model Consultation {
  id String @id @default(cuid())

  companyId String
  company   Company @relation(fields: [companyId], references: [id])

  requestedByUserId String
  requestedBy       UserAccount @relation("ConsultationsRequested", fields: [requestedByUserId], references: [id])

  /// What the customer wants to discuss (required).
  description String

  /// Customer-proposed time slots as free-text or ISO strings. Optional.
  /// Persisted as JSON so an upgraded form can switch to structured slots
  /// without a migration.
  preferredTimes Json?

  /// Customer's timezone at submit-time, e.g. "Europe/Istanbul".
  timezone String?

  /// Admin fills these when scheduling.
  scheduledAt DateTime?
  videoLink   String?
  adminNotes  String?

  /// Google Calendar event id if the booking was auto-scheduled by the
  /// Google integration. Null when the booking is in the legacy PENDING
  /// flow (no Google connected at submit time).
  googleEventId String?

  /// Token cost snapshot (in case admin changes pricing later).
  tokenCost Int

  status ConsultationStatus @default(PENDING)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([companyId, createdAt])
  @@index([status, createdAt])
}

// -----------------------------------------------------------------------------
// ConsultationSettings
// -----------------------------------------------------------------------------
// Site-level configuration for the consultation feature. Managed by
// SITE_OWNER / SITE_ADMIN from /admin/consultations/settings.
//
// Enforced as a singleton via `@unique` on `singleton = true` — Postgres will
// reject a second row since `true` is already taken.
// -----------------------------------------------------------------------------

model ConsultationSettings {
  id        String  @id @default(cuid())
  singleton Boolean @unique @default(true)

  /// Master switch — when false, customer form refuses new bookings.
  enabled Boolean @default(true)

  /// Tokens charged on submit. Replaces the old hardcoded CONSULTATION_TOKEN_COST.
  tokenCost Int @default(50)

  /// Duration (minutes) shown to the customer.
  durationMinutes Int @default(30)

  /// Team contact email (Gmail/etc.) — shown to the customer and used as a
  /// notification target by future integrations.
  contactEmail String?

  /// Public "secret iCal URL" of the team's Google Calendar. Parsed
  /// server-side to surface busy windows on the customer picker.
  calendarIcsUrl String?

  /// Days the team is available (0=Sun ... 6=Sat). Defaults to Mon-Fri.
  workingDays Int[] @default([1, 2, 3, 4, 5])

  /// Working hour window in 24h local time (company timezone).
  workingHourStart Int @default(9)
  workingHourEnd   Int @default(17)

  /// Minimum notice (hours) a customer must give when picking a slot.
  minNoticeHours Int @default(24)

  /// Max days in the future a customer may pick.
  maxBookingDays Int @default(30)

  /// Company timezone, IANA zone. Used when interpreting working hours.
  companyTimezone String?

  /// Free-text notes shown only to admins on the settings page.
  adminNotes String?

  /// Google Calendar OAuth state. Populated when SITE_OWNER / SITE_ADMIN
  /// connects the team Gmail via /admin/consultation-settings/google/connect.
  /// Tokens persisted here plaintext for now — rotating encryption key is a
  /// follow-up; low blast radius since the DB is single-tenant and the
  /// refresh token only scopes calendar.events + calendar.freebusy.
  googleAccountEmail   String?
  googleCalendarId     String?   @default("primary")
  googleAccessToken    String?
  googleRefreshToken   String?
  googleTokenExpiresAt DateTime?
  googleConnectedAt    DateTime?

  updatedAt DateTime @updatedAt

  updatedById String?
  updatedBy   UserAccount? @relation("ConsultationSettingsUpdatedBy", fields: [updatedById], references: [id])
}

// -----------------------------------------------------------------------------
// Admin action audit log (Security Precaution Plan — L1)
//
// Write-only ledger of every privileged action. Rows never update; rows
// never delete (even on UserAccount soft-delete — we anonymize references
// in app code rather than FK-cascade). This is forensic infrastructure:
// the first thing we read after a suspected compromise, and the evidence
// trail for any billing dispute or regulatory audit.
//
// The actor snapshot (actorEmail, actorRole) is denormalized on purpose —
// UserAccount email gets anonymized on GDPR delete, and role may change
// after the action. The log must show who did what at the time, not who
// the actor IS today.
// -----------------------------------------------------------------------------

enum AdminActionType {
  // Withdrawals
  WITHDRAWAL_APPROVE
  WITHDRAWAL_MARK_PAID
  WITHDRAWAL_REJECT
  // Plans & billing
  PLAN_CREATE
  PLAN_EDIT
  PLAN_DELETE
  PLAN_ASSIGN
  COMPANY_TOKEN_GRANT
  // Creative payout rules
  PAYOUT_RULE_EDIT
  // Ticket financial overrides
  TICKET_FINANCIAL_OVERRIDE
  // User / role management
  USER_PROMOTE_TO_ADMIN
  USER_HARD_DELETE
  // Pricing / configuration
  AI_PRICING_EDIT
  CONSULTATION_PRICING_EDIT
  GOOGLE_OAUTH_CONFIG_EDIT
  // Talent applications (SITE_OWNER only — see canManageTalentApplications)
  TALENT_APPLICATION_ACCEPTED
  TALENT_APPLICATION_DECLINED
}

enum AdminActionOutcome {
  SUCCESS // action completed
  BLOCKED // guard rejected (403, validation, etc.) — valuable for recon detection
  ERROR // downstream failure mid-action (DB, external API)
}

model AdminActionLog {
  id String @id @default(cuid())

  // Who performed the action, snapshotted for forensic stability
  actorId    String
  actor      UserAccount @relation("AdminActionLogActor", fields: [actorId], references: [id])
  actorEmail String // snapshot at action time; UserAccount.email may anonymize later
  actorRole  UserRole // snapshot; role may change after the action

  // What the action is + action-specific payload
  action   AdminActionType
  metadata Json? // e.g. { amountTokens, oldPrice, newPrice, … }

  // Target object, if the action has one. Soft-linked (no FK) because the
  // target table varies by action type (Withdrawal, Plan, UserAccount, …).
  targetType String? // "Withdrawal" | "Plan" | "UserAccount" | "Company" | "Ticket" | …
  targetId   String?

  // Request context — where did this come from?
  ipAddress String?
  userAgent String?

  // Outcome (success / blocked / error)
  outcome      AdminActionOutcome
  errorMessage String? // populated when outcome != SUCCESS

  createdAt DateTime @default(now())

  // Query access patterns:
  //   - "what has this owner done" → actorId + createdAt
  //   - "who touched this withdrawal" → targetType + targetId + createdAt
  //   - "show me every MARK_PAID this week" → action + createdAt
  @@index([actorId, createdAt])
  @@index([action, createdAt])
  @@index([targetType, targetId, createdAt])
}

// -----------------------------------------------------------------------------
// MFA challenge table (Security Precaution Plan — L4)
//
// Email-code second factor for money-moving admin actions. When a SITE_OWNER
// tries a money action and has no recent successful MFA, the route issues a
// challenge: generates a 6-digit code, stores the SHA-256 of it here, emails
// the plaintext code, and returns 202 to the client. The client prompts for
// the code and POSTs to /api/admin/mfa/verify which sets `consumedAt`.
//
// "Recent MFA" is just a query: did this user consume any challenge of this
// actionTag within the last N minutes? No separate session table needed.
// -----------------------------------------------------------------------------

model MfaChallenge {
  id     String      @id @default(cuid())
  userId String
  user   UserAccount @relation(fields: [userId], references: [id])

  // actionTag scopes the challenge to a category of action. We use a single
  // tag "MONEY_ACTION" for all L2-protected routes — completing MFA once
  // unlocks withdrawals + token grants + role escalations for the window.
  actionTag String

  // SHA-256 of the 6-digit plaintext code. We never store the code itself.
  codeHash String

  // Issued codes expire quickly (10 min) so a stale code can't be replayed
  // hours later. Separate from the "recent successful MFA" window.
  expiresAt DateTime

  // Set when the code is successfully verified. The presence of a
  // consumedAt within the last 30 min is what "recent MFA" means.
  consumedAt DateTime?

  // Count of wrong-code attempts. At 5, the challenge is locked (further
  // attempts rejected with a clear message, user must issue a new code).
  attempts Int @default(0)

  // Request context snapshot for forensic correlation with AdminActionLog.
  ipAddress String?
  userAgent String?

  createdAt DateTime @default(now())

  // Query patterns:
  //   - "does this user have a recent consumed challenge?" →
  //     userId + actionTag + consumedAt
  //   - housekeeping "delete expired unconsumed" → expiresAt
  @@index([userId, actionTag, consumedAt])
  @@index([expiresAt])
}

/// Idempotency ledger for Stripe webhook deliveries. Stripe retries webhook
/// POSTs on any non-2xx response (for up to 3 days, with exponential backoff).
/// If the first attempt succeeded but the response was lost in transit — or
/// two Stripe workers raced on a concurrent delivery — we'd re-run the
/// handler and double-credit tokens / re-create rows.
///
/// Flow in `app/api/billing/webhook/route.ts`:
///   1. After signature verification, attempt to INSERT this row with
///      `eventId` = the Stripe event's id (which is globally unique).
///   2. P2002 unique-constraint violation → this is a retry of an already
///      processed event → return 200 immediately without running the switch.
///   3. Insert succeeds → run the event handler.
///   4. Handler throws → DELETE the ledger row (best-effort) so the retry
///      that follows is free to reprocess the event.
///
/// Rows accumulate slowly (typically ~hundreds to low thousands per year
/// for a SaaS at our scale). A scheduled cleanup can prune rows older
/// than 14 days — Stripe never retries past 3 days, so 14 days is ample.
model ProcessedStripeEvent {
  /// Stripe event id, e.g. `evt_1OAbcDefGhijKlmn`. Globally unique per Stripe.
  eventId String @id

  /// Stripe event type, e.g. `invoice.payment_succeeded`. Stored for
  /// debugging and for the eventual cleanup cron to report what's being
  /// pruned.
  eventType String

  processedAt DateTime @default(now())

  @@index([processedAt])
}
